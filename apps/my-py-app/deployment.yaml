apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-py-app
  namespace: default
  labels:
    app: my-py-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-py-app
  template:
    metadata:
      labels:
        app: my-py-app
    spec:
      imagePullSecrets:
      - name: harbor-regcred
      containers:
      - name: my-py-app
        image: harbor.patchdb.se/apps/my-py-app@sha256:85202101687d8d1ca9e0788c9a574af30311698d3cfad8ecda1d8ceaa3851f96
        ports:
        - containerPort: 8000
          name: http
        readinessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 20
