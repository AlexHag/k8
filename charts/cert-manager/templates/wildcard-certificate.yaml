apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-domain-certificate
  namespace: envoy-gateway-system
spec:
  secretName: wildcard-domain-tls
  issuerRef:
    name: letsencrypt-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - "*.patchdb.se"
    - "patchdb.se"
