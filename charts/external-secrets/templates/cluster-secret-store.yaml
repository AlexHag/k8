apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: gcp-secret-store
spec:
  provider:
    gcpsm:
      projectID: alexk8s-test-proj
      auth:
        workloadIdentity:
          serviceAccountRef:
            name: {{ (index .Values "external-secrets" "external-secrets" "serviceAccount" "name") }}
            namespace: {{ .Release.Namespace }}
